// RideClash client config.
// Set `backend` to your deployed server URL (e.g. "https://rideclash.onrender.com")
// to switch the site from offline demo mode to live multiplayer mode.
// You can also test without editing this file by running
//   localStorage.setItem("rideclash-backend", "https://your-server")
// in the browser console and refreshing.
window.RIDECLASH_CONFIG = {
  backend: "https://rideclash-server.onrender.com",
};
